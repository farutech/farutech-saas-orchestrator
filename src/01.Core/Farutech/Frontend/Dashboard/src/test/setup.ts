import "@testing-library/jest-dom";

Object.defineProperty(window, "matchMedia", {
  writable: true,
  value: (query: string) => : false,
    media: query,
    onchange: null,
    addListener: () => {},
    removeListener) => tListener: () => {},
    removeEventListen) => hEvent: () => {},
  }),) => 
) => ) => 